matplotlib>=3.3
numpy>=1.24
# ROS2 dependencies (installed via apt, not pip), only needed on the experiment machine:
# sudo apt install ros-humble-rclpy ros-humble-geometry-msgs ros-humble-vrpn-client-ros
